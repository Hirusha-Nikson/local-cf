{
  "name": "@local-cf/demo-worker",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Example Cloudflare Worker with D1 + KV + R2 + Durable Objects + Queues, used to exercise local-cf.",
  "scripts": {
    "demo": "node ../../packages/cli/dist/bin.js dev",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3"
  }
}
